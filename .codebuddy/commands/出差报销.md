---
description: 处理一个包含审批单 PDF 与发票/行程单 PDF 或图片的本地文件夹，调用报销 API 并下载 HOA Excel。
argument-hint: "<本地文件夹路径>"
allowed-tools: Read, Bash
---

请处理这个出差费用报销文件夹：
`$ARGUMENTS`

要求：

1. 优先使用当前项目中的 `business-trip-expense` skill。
2. 如果没有提供路径，先请用户补充一个本地文件夹路径。
3. 调用技能脚本递归扫描文件夹，上传审批 PDF 与发票/行程单 PDF 或图片，并下载 `HOA-001` 和 `HOA-002`。
4. 回复时给出 `session_id`、预览摘要和生成文件路径。
5. 如果失败，说明失败原因。
